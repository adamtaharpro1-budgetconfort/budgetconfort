# Database (Neon Postgres via Vercel Marketplace)
DATABASE_URL=""
DIRECT_URL=""

# Auth.js
AUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (added in V2 once credentials are provided)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Email (verification + password reset) via Resend
RESEND_API_KEY=""
EMAIL_FROM="LifePilot AI <onboarding@resend.dev>"

# Vercel AI Gateway (auto-provided via OIDC on Vercel deployments)
AI_GATEWAY_API_KEY=""

# Vercel Blob (photo uploads: recipes, pantry, receipts, avatars)
BLOB_READ_WRITE_TOKEN=""
